using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ForestIsland : IslandElement
{
    // Start is called before the first frame update
    void Start()
    {
        Init("Target Estelle");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
