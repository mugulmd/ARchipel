using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TempleIsland : IslandElement
{
    // Start is called before the first frame update
    void Start()
    {
        Init("Target Rorie");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
